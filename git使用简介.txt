配置全局用户名以及邮箱：
git config --global user.name "yuldong"
git config --global user.email "yuldong@126.com"

查看配置信息：
git config -l

查看当前仓库状态：
git status

提交之前将文件添加到暂存库
git add fileName

将文件提交至主干
git commit -m “注释”

综合提交并注释
git commit -a -m “注释”

查看提交日志信息
git log fileName

将所有文件添加到暂存库中
git add .
